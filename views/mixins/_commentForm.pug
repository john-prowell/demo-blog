mixin commentForm(post)
  form.reviewer(action=`/comments/${post._id}` method='POST')
    textarea(name='text' placeholder='Did you like this post? Have something to say? Leave a comment...')
    input.button(type='submit' value='Submit Review')
