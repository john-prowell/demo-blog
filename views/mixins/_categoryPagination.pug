mixin categoryPagination(page, pages, count)
  //-pre=h.dump(count)
  .pagination
    .pagination__prev
      if page > 1
        a(href=`/category/${category}/page/${page - 1}`) Prev
    .pagination__text
      p Page #{page} of #{pages} — #{count} total results
    .pagination__next
      if page < pages
        a(href=`/category/${category}/page/${parseFloat(page) + 1}`) Next